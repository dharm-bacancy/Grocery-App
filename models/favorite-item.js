class FavoriteItem {
    constructor(itemPrice,itemTitle,itemDetail,itemImage) {
        this.itemPrice = itemPrice;
        this.itemTitle = itemTitle;
        this.itemDetail = itemDetail;
        this.itemImage = itemImage;
    }
};

export default FavoriteItem;